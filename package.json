{
  "name": "rdscom",
  "version": "1.0.2",
  "description": "A redis based communication library for microservices",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node14 --outfile=dist/index.js --minify --sourcemap && cp src/index.d.ts dist/index.d.ts",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/jest": "^29.5.13",
    "@types/uuid": "^10.0.0",
    "ioredis": "^5.4.1",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.6.3"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  }
}
